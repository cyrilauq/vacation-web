export default class User {

    constructor(name, id, firstname, mail, token) {
        this.name = name
        this.id = id
        this.firstname = firstname
        this.mail = mail
        this.token = token
    }

}